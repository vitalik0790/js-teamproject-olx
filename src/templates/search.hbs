{{#each this}}
<li class="search__goods" data-category="{{category}}" data-id="{{_id}}">
  <img class="search___img" src="{{imageUrls}}" alt="{{description}}" width="240" height="240" />

  <div class="search__info">
    <p class="search__title">{{title}}</p>
    <p class="search__price">{{price}} €</p>

  </div>
</li>
{{/each}}